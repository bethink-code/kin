# Project skills

Custom, project-scoped skills for Kin live here. Each skill is a folder with a `SKILL.md` describing when and how to invoke it.

Global skills (security-guardian, pre-commit-check, simplify, etc.) live in `~/.claude/skills/` and apply to every project — don't duplicate them here.
