# This project uses Doppler for secrets.
# DO NOT create a .env file. Secrets are injected at runtime by `doppler run --`.
# This file documents the variable names only.

DATABASE_URL=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SESSION_SECRET=
ADMIN_EMAIL=
NODE_ENV=
ANTHROPIC_API_KEY=
